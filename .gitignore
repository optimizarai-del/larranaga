# Python
backend/.venv/
__pycache__/
*.py[cod]
backend/*.db-shm
backend/*.db-wal
backend/*.sqlite
backend/.env

# SECRET_KEY autogenerada (cuando no hay env var SECRET_KEY)
backend/app/.secret_key
.secret_key

# AFIP SDK — certificados y claves privadas por CUIT/entorno
backend/afip_certs/

# Claude Code — solo el settings local (el resto, incluidos skills, se versiona)
.claude/settings.local.json
.claude.rar

# AFIP SDK — token de app.afipsdk.com (archivo con el access_token en texto plano)
AFIP_SDK_ACCESS_TOKEN/

# Node
frontend/node_modules/
frontend/dist/
frontend/.env

# OS
.DS_Store
Thumbs.db
*.log
