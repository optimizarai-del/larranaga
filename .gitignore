# Python
backend/.venv/
__pycache__/
*.py[cod]
backend/*.db-shm
backend/*.db-wal
backend/*.sqlite
backend/.env

# AFIP SDK — certificados y claves privadas por CUIT/entorno
backend/afip_certs/

# Claude Code — solo el settings local (el resto, incluidos skills, se versiona)
.claude/settings.local.json
.claude.rar

# AFIP SDK — token de app.afipsdk.com (archivo con el access_token en texto plano)
AFIP_SDK_ACCESS_TOKEN/

# Node
frontend/node_modules/
frontend/dist/
frontend/.env

# OS
.DS_Store
Thumbs.db
*.log

# Credenciales locales — NUNCA commitear
Credenciales_*.txt
afip_dev_credentials.txt
*_credentials*.txt
*_passwords*.txt

# Carpeta OneDrive accidentalmente incluida
OneDrive/

# SQLite WAL/SHM (archivos de transacción en vuelo)
backend/larranaga.db-shm
backend/larranaga.db-wal

# Cloudflare Tunnel — token contiene credenciales privadas del túnel
.env
cloudflared/credentials.json
cloudflared/*.json

# Backups locales
docker-compose.yml.backup
