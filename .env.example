# ─── Cloudflare Tunnel ────────────────────────────────────────────────────
# Token del túnel — se obtiene desde el dashboard de Cloudflare:
#   Zero Trust → Networks → Tunnels → [tu túnel] → Configure → Token
# Este token incluye credenciales del túnel: NUNCA commitearlo.
CLOUDFLARE_TUNNEL_TOKEN=
